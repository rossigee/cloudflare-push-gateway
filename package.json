{
  "name": "cloudflare-push-gateway",
  "version": "0.1.1",
  "description": "Cloudflare Workers-based Prometheus Pushgateway",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env \"\"",
    "build": "wrangler deploy --dry-run --env \"\"",
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.3",
    "@cloudflare/workers-types": "^4.20250401.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.5",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "jose": "^6.2.3"
  }
}
